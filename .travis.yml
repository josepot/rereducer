language: node_js
node_js:
  - "node"
install:
  - npm i -g npm@5.8.0
  - npm ci
script:
  - npm run lint
  - npm run format:check
  - npm run test
  - npm run test:types
branches:
  only:
    - master
cache:
  directories:
    - "$HOME/.npm"
